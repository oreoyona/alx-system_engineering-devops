#include "main.h"
/**
 * _strcat - joins 2 strings
 * @dest: the first string
 * @src: the second string
 */
char *_strcat(char *dest, char *src)
{
	int i;

	for (i = 0; i < *dest[i]; i++)
	{
	}
	char string1[i] = *dest;
	int l;

	for (l = 0; l < *src[l]; l++)
	{
	}
	char string2[l] = *src;
	int x;
	char y[l + i];

	for (x = 0; x < l + 1; x++)
	{
		y += string2[x];
	}

	char *concat = &y;

	return (concat);
}
